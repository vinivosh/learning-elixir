defmodule IdenticonTest do
  @moduledoc """
  Tests for the **Identicon** module.
  """

  use ExUnit.Case
  doctest Identicon

  # test "greets the world" do
  #   assert Identicon.hello() == :world
  # end
end
